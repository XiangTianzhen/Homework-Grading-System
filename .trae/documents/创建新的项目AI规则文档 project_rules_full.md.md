## 目标
- 基于你列出的全部对话与当前代码，创建一份更全面的 AI 规则提示词文档，帮助其他 AI 快速理解项目并协助开发。
- 保留现有 `project_rules.md`，新增文档：`c:\Projects\作业批改系统\.trae\rules\project_rules_full.md`。

## 文档结构
1. 项目概览：项目名称、论文主题、主要目标与范围、受众水平（初中生级别，非企业级）。
2. 技术栈与架构：前端/后端/OCR，目录位置与职责。
3. 前端路由与页面：`/workspace` 与 `/generator`、`AppShell.vue` 导航约束。
4. 组件与状态：关键组件列表与 localStorage 键（`errorBook`、`gradingHistory`）。
5. 试卷图片生成器：折叠设置、下划线占位、区域导出、参数范围、调试叠加。
6. OCR 工具与配置：百度 OCR 端点、参数、环境变量设置（PowerShell）、官方链接与 token 缓存策略。
7. 后端结构与 API 接口：`server.js`、`services/baiduOCR.js`、`utils/logger.js`；详细接口说明与请求/响应示例、日志事件。
8. 日志规范：路径、命名格式、记录项。
9. 开发环境与代理：Vite 前端、端口、API 前缀 `/api/*` 与代理到后端 `http://localhost:3000`。
10. Git 提交与远程同步：中文语义化规范、类型与模板、分支与 PR、远程名与推送命令；PowerShell 链式分隔注意事项。
11. 样式与 SCSS 嵌套规范：全站嵌套、变量/混入来源、移动端与4K适配。
12. 注释规范：JS/SCSS 简洁说明性注释。
13. 调试工具与验证：显示区域框、OCR结果面板、单题重识别建议。
14. 安全与隐私：不提交密钥，使用环境变量。
15. 变更管理：更新规则与README的同步。

## 执行
- 新增文件并写入上述结构内容；不修改原文档。

## 后续
- 完成后若发现 README 需要补充链接或说明，我会提出建议供你选择是否更新。